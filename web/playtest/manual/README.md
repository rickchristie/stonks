# Manual Playtests

Temporary exploratory specs can live here. They are ignored unless `PLAYWRIGHT_INCLUDE_MANUAL=1` is set.

Promote useful probes into normal specs before treating them as regression coverage.
